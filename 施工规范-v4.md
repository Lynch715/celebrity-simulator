# 《模拟明星》v4 施工规范

> 实施者：Opus 5。实施《设计文档-v4迭代建议.md》前通读本规范。目标文件：`模拟明星.html`（唯一交付物）。

## 1. 架构红线

1. **保持单 HTML 文件**。不拆 CSS/JS，不引外部依赖，不加 CDN。
2. **不改** `SAVE_KEY`、既有结局标题字符串、成就 `id`、`playerScore` 公式、`MAX_MONTH`。成就与结局判定散布多处按标题字符串匹配，改名即断档。
3. 新代码追加在 `<script>` 内对应模块区，用既有分区注释风格：`/* ══════════════ N、模块名 ══════════════ */`。
4. 新 CSS 追加在 `/* ===== 扩展包 v3 样式 ===== */` 之后，新起 `/* ===== v4 样式 ===== */` 区。禁止修改既有选择器，只允许追加。

## 2. 存档兼容（最高优先级）

- 每批次上线时 `SAVE_VERSION` +1（当前为 3）。
- **所有新增状态字段**必须在 `load()` 的迁移处补默认值，模式：
  ```js
  if(!s.studio)s.studio={level:0,invested:0};
  if(!s.mastery)s.mastery={};
  if(!s.foreshadow)s.foreshadow=[];
  ```
- 迁移必须幂等（重复执行无副作用）。禁止任何会让旧档报错或数值突变的写法。
- meta 存档（名人堂/成就/传承）同理：`loadMeta()` 中缺省补全，新字段有旧档缺失时的兜底。
- **每批次验收必测**：用 v3 存档（含月中、有进行中项目、有 romance 的档）载入新版本，跑满 3 个月无报错。

## 3. 数值与状态操作

- 改属性一律走既有 helper：`add(s,key,v)`（自动 clamp）、`risk(s,key,v)`（自动吃难度系数）、`relation(s,id,v)`、`clamp/round/pick/rand`。**禁止**直接 `s.fame += x`。
- 一切正向风险增量自动经过 `DIFF(s).riskMul`（`risk()` 已处理，不要绕过）。新增的现金收益如属「片酬类」需乘 `DIFF(s).cashMul`。
- 概率判定用 `Math.random()`，成功率写成常量 + 修正项的形式并留注释，如 `.42 + pr*.25 /* 基础42%，宣传能力加成 */`。
- 数值边界：风险 0-120，属性 0-100，现金 -999~9999（`add` 已处理）。新字段自行 clamp。
- 设计文档给出的数值是**基准值**，实施时允许 ±20% 内微调，超出需在代码注释中说明理由。

## 4. 事件与弹窗

- 事件对象结构：`{id, title, body|body(), opts:()=>[eventOption(text, sub, fn, disabled)]}`。
- 弹窗只用 `showModal` / 队列用 `queueAll`+`flushQueue`；月末新增检查一律插入 `doEndMonth` 的 `tasks` 数组，注意顺序：结算报告 → 项目完成 → 事故 → 灾难 → 风险征兆/事件 → 粉圈 → 擂台 → 危机 → 年度 → 下月。新系统按语义插入对应位置（片场事件放在结算报告后）。
- 每个 modal 必须有一个不花钱不消耗的退路选项（可以有代价，但不能软锁）。花钱选项必须带 `disabled: S.cash < 费用`。
- 单局去重进 `s.usedEvents`；危机类事件接入 `s.priors`（旧事重提机制）。
- 轻反馈用 `toast(msg, "good|bad|warn")`；舆论类事件配 `tickerPush` 飘屏；重大时刻（专精解锁、Lv3 工作室、CP官宣）配 `splash`。负面冲击配 `shake()`。

## 5. 文案规范

- 语气对齐现状：第二人称、克制、行业感，不玩烂梗，不用感叹号堆情绪。参考基准：「收入很少，公众会观察你是真心还是摆拍。」
- 选项 sub 必须写清代价与收益，格式如 `花12万，路人缘+5`；隐藏效果（如 progress）不写数字，用定性词（「进度顺利」）。
- 全角标点；数字与「万」之间不空格；NPC 名字用现有角色，新增 NPC 需在设计文档层面确认，本轮不新增。
- 涉及敏感题材（税务、灰色操作、粉圈互撕）保持现有的批判性距离感，不美化。

## 6. UI 规范

- 颜色只用 CSS 变量：`--gold --red --green --cyan --muted --panel --line` 等。
- 新面板复用既有组件类：`.metric` `.card` `.choice` `.rank-table` `.section-head`。
- 移动优先：360px 宽度下不得横向溢出（`html,body{overflow-x:hidden}` 是兜底不是许可）；点击热区 ≥32px。
- tabs 当前为 8 列 grid，本轮**不新增页签**；新功能挂进现有页签（工作室→团队页，专精→技能区，预感→资料页）。
- 交互文本 `esc()` 转义后再插入 innerHTML（现有惯例，见 `tickerPush` 用法）。

## 7. 分期施工与提交

- 严格按设计文档第五节批次执行，**一个批次一次交付**，批次内完成全部验收项再进下一批。
- 每批次交付时在 HTML 头部注释追加变更记录一行：`<!-- v4批次1 2026-xx-xx 训练递减+专精+工作室 SAVE_VERSION=4 -->`。
- 改动前先通读将要挂接的函数全文（`settleCore`、`doEndMonth`、`completeProject`、`load` 是高危区），确认插入点不破坏既有分支。
- 禁止顺手重构、顺手改文案、顺手调无关数值。发现既有 bug 单独记录在文件末尾注释 `/* KNOWN ISSUES */` 中，不擅自修。

## 8. 测试清单（每批次）

1. 新开局 × 4 背景 × 3 难度，各推进 3 个月无报错（控制台零 error）。
2. v3 旧档载入（见第 2 条），推进 3 个月。
3. 完整速通一局至 36 月（可临时在控制台注入快进，**交付代码中不得残留任何 debug 函数/按钮**）。
4. 本批次新功能的每个分支至少手动触发一次（含钱不够时的 disabled 态）。
5. 360px 视口过一遍受影响页面。
6. 结局回归：至少触发 1 个好结局 + 1 个坏结局，确认成就与名人堂正常写入。

## 9. 验收标准汇总（对应设计文档）

| 项 | 验收 |
|---|---|
| P0-1 | 36 月一局片场事件 6-10 次；progress 对 quality 影响 ≤±9；每类型池 ≥4 条文案 |
| P0-2 | 三级费用 40/90/180；独立工作室 8 折；Lv3 自制项目盈亏三档生效 |
| P0-3 | 三档递减生效；5 技能 ×2 专精全部可触发且互斥 |
| P1-1 | 限时项目 UI 标记；抢角失败对手 score+4 且飘屏播报 |
| P1-2 | 8 类风险 ×2 条伏笔文案；3 个月内压回 40 以下则静默移除 |
| P1-3 | 结局页三选一遗物；名人堂徽章展示；下局生效 |
| P1-4 | 营业期收益/代价逐月生效；与 romance、devoted 人设的联动生效 |
| P2-* | 见设计文档各条 |
