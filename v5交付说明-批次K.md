# 《模拟明星》v5 · 批次K：选择反馈

> `模拟明星.html` 614 KB。**`SAVE_VERSION` 仍是 13**——纯表现层，没有新增存档字段，旧档直接可用。
> 备份：`.bak-v5J`。`index.html` 已同步。

---

## 一、问题

扫了一遍：**355 个选项里只有 31 个（9%）写了 `gameLog`，而日志在另一个页签；其余 91% 点完就静默关窗。**没有一个选项用过 toast。

也就是说玩家选「陪剧组熬完夜戏」，弹窗消失，然后什么都没有。他得自己去比对顶部那几个数字，或者切到日志页翻。

## 二、做法：不逐条改选项，在必经之路上做前后快照

355 个选项逐个补反馈既费事又必然漏。改成在**点击这个动作的必经之路**上做自动对比：

1. 点击前给整个状态拍一张快照（8 项主数值、8 类风险、5 个技能、18 个人物好感、人设契合度与信仰粉、恋爱信任、粉圈四项、预备金、灰色记录、提名与奖项数、日志长度）
2. 跑选项的 `fn()`
3. 对比出差值，同时把这期间新写进日志的那句叙述捞出来

**作者已经写好的那句话不用重写**——`gameLog` 里那句「你把那场夜戏拍到了导演满意为止」本来就在，只是以前只有翻日志才看得到。

挂了两个点，覆盖全部路径：

- **弹窗选项的 onclick**——覆盖所有事件、片场、剧情线、恋爱、擂台、专精等 355+ 个选项
- **内联 onclick 按钮**（`fanAct`／`handleTrend`／`interactNpc`／`acceptProject`／`verifyTrend`／`studioUpgrade`／`loveMeet` 等 23 个）——这些走 `globalThis`，统一包了一层

差值为空就什么都不显示，所以「取消」「再想想」这类选项仍然安静。

## 三、显示在哪：分两种情况

**选完不再弹窗** → 底部浮出一条，叙述在上、数值胶囊在下，4.2 秒后淡出，点一下立刻消掉：

> 《雨夜名单》：你把那场夜戏拍到了导演满意为止。
> `健康 -6`（红） `顾为民 +4`（绿）

**选完紧接着又弹了一个窗** → 不再浮层，**直接把胶囊贴进那个弹窗正文下面**（加一条分隔线）。第一版做成浮层时它会压在弹窗按钮上，而且弹窗正文本来就在讲同一件事，重复且挡路。

配色按语义走，不是按正负：风险、黑粉热度、灰色记录**涨了标红**，跌了标绿；其余数值反过来。

## 四、几个实际效果

| 操作 | 改前 | 改后 |
|---|---|---|
| 事件「单独出席饭局」 | 静默 | `商业价值+4` `合同隐患+9` `私生活隐患+10` `灰色记录+1` `周岭+9` |
| 「集中训练表演」 | 静默 | `健康-1` `心理-2` `表演+3` |
| 片场「留在片场陪拍完」 | 只写日志 | 叙述 +『健康-6』『顾为民+4』 |
| 热搜「发声明」 | 只有一段文字 | 文字下方补 `路人缘+3` `粉丝+2` |
| 粉圈「扶持数据组」 | 一条 toast | 叙述 + `现金-6万` `知名度+2` `数据组+1` `粉圈隐患+7` `黑粉+4` |

## 五、验收

| 项 | 结果 |
|---|---|
| 语法 | `node --check` 通过 |
| **反馈覆盖率** | 抽样 220 个选项逐个执行并比对：**100%（220/220）能给出反馈**，改前 9% |
| 组合回归 | 768 组合 × 36 月，每月额外跑一次快照与差值计算，**错误 0** |
| 弹窗遮挡 | 后接弹窗时胶囊贴进正文，实测不遮按钮；无后续弹窗时浮层在底部且可点掉 |
| 存档 | 无新增字段，`SAVE_VERSION` 保持 13，旧档不受影响 |
| 横向溢出 | 6 个页签均无 |
| 浏览器 | 零 JS 错误 |

## 六、还可以往下做

- 现在是**单次操作**的反馈。可以再加一层**「本月小结」**：结束本月时把这一个月所有操作的净变化合并成一张卡（现在月末只有收支报告，没有「你这个月一共换来了什么」）。
- 胶囊目前最多显示 12 条，超出截断。真要溢出的场景很少（最多的一次是 5 条），但可以加个「+3 项」的折叠。
